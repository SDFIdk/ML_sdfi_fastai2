# Extra pip packages so the same env runs all four repos (ML_Production, ML_geo_production, ML_sdfi_fastai2, multi_channel_dataset_creation).
# Install after requirements_pip.txt from ML_sdfi_fastai2 and install_local_repos.sh from ML_Production.
paramiko
sqlalchemy
tabulate
geoalchemy2
# Pin pyproj for compatibility with conda proj's proj.db (layout version 5). Newer pyproj expects proj.db layout >= 6.
pyproj==3.6.1
# For ML_sdfi_fastai2 branch SOTA_models_2026 (ConvNeXt V2 + UPerNet and other SOTA configs).
transformers
